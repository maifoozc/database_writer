# MQTT broker (required on private server)
MQTT_BROKER_HOST=127.0.0.1
MQTT_BROKER_PORT=1883
# Use # to receive all levels (e.g. ems/site/1/meter_data); + only matches one level
MQTT_TOPIC=ems/site/#

# Optional auth
# MQTT_USERNAME=
# MQTT_PASSWORD=
# MQTT_USE_TLS=0
# MQTT_CLIENT_ID=mqtt_to_db

# PostgreSQL (required) — replace host with your DB server: localhost, 127.0.0.1, or server IP/hostname
DATABASE_URL=postgresql://service_user:service_pass@127.0.0.1:6432/ems_db

# Optional
# DB_BATCH_SIZE=500
